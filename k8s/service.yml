apiVersion: v1
kind: Service
metadata:
  name: okkkk
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "okkkk"
    port: 8080
  selector:
    app: okkkk
